# backend/requirements.txt

# --- Core Framework ---
fastapi
uvicorn[standard]  # ASGI server with high-performance extras
python-dotenv      # For loading .env files in local development

# --- Data & API Interaction ---
httpx              # Modern async HTTP client for calling external APIs
pandas             # Powerful library for data manipulation and cleaning
census             # A convenient Python wrapper for the U.S. Census API

# --- Database (PostgreSQL + SQLAlchemy) ---
sqlalchemy        # The ORM for interacting with our database
psycopg2-binary   # PostgreSQL database adapter for Python
alembic           # For database migrations (good practice)

# --- Geocoding & Utilities ---
geopy              # For any potential distance calculations or fallback geocoding